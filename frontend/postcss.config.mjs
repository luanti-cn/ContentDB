/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Tailwind CSS v4:PostCSS 插件已移到独立包 @tailwindcss/postcss
    "@tailwindcss/postcss": {},
  },
};

export default config;
